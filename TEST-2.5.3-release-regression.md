# Выпуск 2.5.3 — точечная регрессия

Дата: 2026-09-13. Целевые выполненные проверки — PASS.
Это не полный прогон S01–S10 и не полный passed-журнал. Реальные кластеры,
серверные пакеты и службы не изменялись; установка релизного DEB не выполнялась.
Перечисленные проверки завершились до указания пользователя не выполнять
тестирование. После этого указания новые тесты не запускались.

## Проверки и время

Время указано в MSK (UTC+03:00), длительность — по меткам запуска и завершения
цепочки. Два стенда проверялись параллельно; общий интервал — 6 секунд,
18:22:57–18:23:03. Подготовка документации и последующая сборка сюда не входят.

| WSL | Начало — конец | Длительность | Release fixtures | CLI/ENV | Syslog-ng |
|---|---|---|---|---|---|
| alse-1.8.6-mg16.5.0 | 18:22:57–18:23:02 | 5 с | PASS | 131 PASS | 11 fixture PASS + 3 проверки настоящим syslog-ng PASS |
| mint22-3 | 18:22:59–18:23:03 | 4 с | PASS | 131 PASS | 11 fixture PASS |

Release fixtures проверяют syntax/version/help всех трёх сценариев, модели
delete/rename/start-stop/menu/Tantor, временные каталоги builder и конфиги.
Syslog fixtures проверяют ограничение WSL, сохранение рабочих парсеров и
пользовательских конфигов, восстановление исходника при ошибке проверки,
идемпотентность, обновление путей, отказ от опасных путей и ссылок.
На Astra дополнительно проверены синтаксис, диагностика отсутствующего парсера
и откат при постороннем отсутствующем парсере настоящим syslog-ng с изолированным
конфигом. На Mint эти три проверки **не тестировались**: /usr/sbin/syslog-ng
не установлен; пакет ради проверки не устанавливался. Пропуск не является FAIL.
Предупреждение Mint о LANG с лишними кавычками не считается дефектом согласно
согласованному исключению; возврат к локали C не помешал проверкам.

Команды: существующие tools/prx-test-release-fixtures.sh (версия 2.5.3),
tools/prx-test-full-contracts.sh и tools/prx-test-wsl-syslog.sh.
Логи: tmp/release-2.5.3-20260913/<WSL>/{fixtures/check.log,contracts/,syslog.log}.
Успех fixture не доказывает живое развёртывание или доставку журнала демоном.

## Дистрибутив

Пакет dist/claster-creator-2.5.3.deb собирается штатным builder в режиме 1
в отдельном каталоге на Linux-ФС. Конфиг берётся из предыдущего DEB 2.5.2,
а не из локальной пользовательской .new-claster.config. Тесты содержимого
и установки итогового DEB 2.5.3 не выполняются по указанию пользователя;
автоматический release-helper с тестовыми проверками для этой сборки не запускается.

Включён фактический TEST-2.5.2-journal.md с 32 PASS и 27 неполностью проверенными
ID; его версия и результаты не меняются. Этот журнал относится к рабочим
исходникам 2.5.2 с тем же исправлением syslog-ng, а не к полному тесту 2.5.3.
При подготовке версии 2.5.3 исполняемая логика трёх сценариев не менялась;
изменены версия и документация, отдельная побайтовая проверка пакетов не проводилась.
Старый release-отчёт 2.5.2 перенесён в
игнорируемый tests/; в корне остаётся этот отчёт и последний журнал.
